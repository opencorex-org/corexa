{
  "name": "corexa",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "format": "biome check --write .",
    "changeset": "changeset",
    "desktop:dev": "pnpm --filter @corexa/desktop dev",
    "cli:dev": "pnpm --filter @corexa/cli dev",
    "docs:dev": "pnpm --filter @corexa/docs-site dev",
    "docs:build": "pnpm --filter @corexa/docs-site build",
    "runtime:dev": "go -C runtime/daemon run ./cmd/corexad",
    "runtime:test": "go -C runtime/daemon test ./...",
    "native:test": "cargo test --manifest-path native/tree-sitter-indexer/Cargo.toml"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@changesets/cli": "^2.28.1",
    "turbo": "^2.0.12",
    "typescript": "^6.0.3"
  }
}
